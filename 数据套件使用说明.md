<!--

SPDX-License-Identifier: CC-BY-NC-SA-4.0

本文件是“汉末废柴堆 · 超级团队组建系统”的一部分。

完整项目受 CC BY-NC-SA 4.0 许可证保护，详见根目录 LICENSE 文件。

本项目核心设计思想、架构方案、方法论受 NOTICE 文件独立保护。

个人可免费使用，严禁用于商业目的。

商业授权请联系：xxuuyyuunn@sina.com

-->

# 超级团队数据套件使用说明

**版本历史**

| **版本号** | **过程节点说明** | **修订人** | **修订日期** | **修订内容**         |
| ---------- | ---------------- | ---------- | ------------ | -------------------- |
| V0.1.0     | 初稿创建         | 徐昀       | 2026-06-01   | 完成文档初始版本撰写 |
| V0.1.1     | 内容简化         | 徐昀       | 2026-06-01   | 内容简化，重绘流程图 |
|            |                  |            |              |                      |

## 一句话定位

**知识图谱闭环**：上传文档 → 解析成知识图谱 → 可视化调整 → 本地RAG检索问答

**数据分析闭环**：上传数据 → 生成图表配置 → 拖拽布局大屏 → 深度分析报告

## 使用方法

**你会使用DeepSeek吗？**

**你会上传文件到DeepSeek对话窗口吗？**

**OK，你已经学会了全部用法。**

> 打开DeepSeek，上传`prompt.md`和`characters_汉末废柴堆.md`两个文件，发送，即可开始体验。
>
> 使用过程中，可在需要时上传相应插件。
>
> 为防止超出对话长度，保证对话质量，建议每个对话使用不超过3个插件。

## 核心思想

数据套件由**两个闭环、四个插件、两个画布**组成。

### 知识图谱闭环

将非结构化的文档转化为可检索、可编辑、可问答的知识图谱。

**三步走**：

1. **解析**：用**莱昂差不多**将任意文档（Word/PDF/PPT/纯文本）解析为标准JSON格式（nodes + edges）
2. **编辑**：将JSON导入**最后的图谱**画布，可视化调整节点位置、修改属性、增删关系，导出优化后的JSON
3. **检索**：将JSON交给**格兰三缺一**，用自然语言提问，插件三人组（哈利、赫敏、罗恩）协作在图谱中检索答案，附带完整检索依据

```mermaid
flowchart TB
    subgraph Step1[第一步：解析文档]
        S1_1[用户：上传Word/PDF/PPT/文本] --> S1_2[@达芬奇]
        S1_2 --> S1_3[莱昂差不多<br/>六步拆解+双模式分类]
        S1_3 --> S1_4[输出知识图谱JSON<br/>nodes, edges]
    end

    subgraph Step2[第二步：可视化编辑]
        S2_1[用户：打开最后的图谱] --> S2_2[导入JSON]
        S2_2 --> S2_3[拖拽节点调整位置<br/>修改属性/状态/类型<br/>增删节点和关系]
        S2_3 --> S2_4[右键菜单导出优化后JSON]
    end

    subgraph Step3[第三步：本地RAG检索]
        S3_1[用户：上传JSON到DeepSeek] --> S3_2[格兰三缺一<br/>赫敏建立索引]
        S3_2 --> S3_3[用户：提问<br/>“深度学习有哪些应用？”]
        S3_3 --> S3_4[哈利：匹配起始节点<br/>赫敏：图遍历检索<br/>罗恩：润色答案]
        S3_4 --> S3_5[输出答案 + 完整检索依据<br/>起始节点/路径/命中节点/实际跳数]
    end

    Step1 --> Step2 --> Step3
```

### 数据分析闭环

将数据转化为可视化大屏，并生成深度分析报告。

**三步走**：

1. **生成配置**：用**米开始摸鱼**根据数据或描述，自动生成ECharts图表配置JSON
2. **布局大屏**：将多个图表JSON导入**西斯廷看板**画布，拖拽调整位置大小，导出看板JSON
3. **分析报告**：将看板JSON交给**侦探团本团**，三人组（阿笠博士、灰原哀、柯南）协作输出诊断、预警、预测、干预建议的完整报告

```mermaid
flowchart TB
    subgraph Step1[第一步：生成图表配置]
        D1_1[用户：提供数据或描述] --> D1_2[@米开 或 @米开朗基罗]
        D1_2 --> D1_3[米开始摸鱼<br/>智能推荐图表类型]
        D1_3 --> D1_4[输出ECharts JSON配置<br/>含样式+工具箱]
    end

    subgraph Step2[第二步：大屏布局]
        D2_1[用户：打开西斯廷看板] --> D2_2[导入多个图表JSON]
        D2_2 --> D2_3[拖拽调整大小位置<br/>自由布局]
        D2_3 --> D2_4[右键菜单导出看板JSON]
    end

    subgraph Step3[第三步：分析报告]
        D3_1[用户：上传看板JSON或数据文件] --> D3_2[侦探团本团<br/>阿笠博士校验数据]
        D3_2 --> D3_3[灰原哀提取特征+红黄绿灯]
        D3_3 --> D3_4[柯南推理+预测+干预建议]
        D3_4 --> D3_5[输出《数据分析报告》<br/>含发展方向预测/预警/干预建议]
    end

    Step1 --> Step2 --> Step3
```

### 关于输出：

DeepSeek支持输出HTML格式文档，可下载后使用浏览器打开浏览，或使用Word编辑，为保证格式和风格一致，可使用插件（**文心雕文龙**）生成HTML文档。

### 关于功能扩展：

Deepseek提供的所有功能均可使用团队完成，如果希望提高效率，可使用系统扩展机制（**盘古开插件**）自行开发新的插件。

所有画布为HTML文件，如需增加功能，可将画布文件交给AI，在此基础上继续开发。

## 核心优势

### 对使用者而言

1. **闭环完整**：从原始输入到最终产出，每一步都有工具支撑
2. **可视化编辑**：图谱和大屏都支持拖拽调整，不用手写代码
3. **本地RAG**：知识图谱检索不依赖外部API，数据安全可控
4. **专业分析**：侦探团本团三人协作，输出含置信度、预警、干预建议的专业报告
5. **零成本**：个人使用完全免费，全部在DeepSeek+浏览器完成

### 对开发者而言

1. **标准化接口**：所有插件和画布使用统一的JSON格式（nodes+edges / ECharts option）
2. **解耦合设计**：各组件可独立使用，也可串联形成闭环
3. **可扩展**：可基于现有格式开发新的插件或画布

## 插件与画布列表

- 莱昂差不多：一个以达·芬奇为主持人的文本/文件→知识图谱JSON智能解析器——关系驱动、不做层级假设，把用户输入的自然语言自动拆解成标准的知识图谱格式。
- 最后的图谱：一个支持多页签、可视化知识图谱编辑器的HTML工具——可导入/导出JSON、自由增删节点与关系、实时编辑属性与样式，并支持status状态边框映射。
- 格兰三缺一：一个由哈利·波特、赫敏·格兰杰、罗恩·韦斯莱三人协作的知识图谱RAG检索插件——上传任意结构的JSON图谱，三人分工检索，输出带完整检索路径的正确答案。
- 米开始摸鱼：一个由米开朗基罗驱动的ECharts图表配置智能助手——根据用户提供的数据和需求，自动推荐最佳图表类型、补全套样式、输出可直接使用的JSON配置，并提供可调整项清单。
- 西斯廷看板：一个支持多页签、可拖拽布局的ECharts仪表盘编辑器——自由添加图表卡片、导入/编辑JSON配置生成图表，支持导出大屏配置和截图，卡片位置和大小可随意拖拽调整。
- 侦探团本团：一个由阿笠博士、灰原哀、江户川柯南三人协作的数据分析与可视化大屏解读团队——上传任意格式文件，自动抽取数据，输出包含诊断、预警、预测、干预建议的完整报告。



